@using FinancialWeb.ViewModels.Transaction
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model TransactionSummaryViewModel
@{
    ViewData["Title"] = "Báo cáo hệ thống";
}

<div class="container py-4">
    <h1>Báo cáo tài chính hệ thống</h1>

    <div class="row mb-4">
        <div class="col-md-4">
            <div class="card bg-success text-white">
                <div class="card-body">
                    <h5>Tổng thu toàn hệ thống</h5>
                    <h3>@Model.TotalIncome.ToString("N0") VND</h3>
                    <p>@Model.StartDate.ToString("dd/MM/yyyy") - @Model.EndDate.ToString("dd/MM/yyyy")</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card bg-danger text-white">
                <div class="card-body">
                    <h5>Tổng chi toàn hệ thống</h5>
                    <h3>@Model.TotalExpense.ToString("N0") VND</h3>
                    <p>@Model.StartDate.ToString("dd/MM/yyyy") - @Model.EndDate.ToString("dd/MM/yyyy")</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card bg-primary text-white">
                <div class="card-body">
                    <h5>Số dư toàn hệ thống</h5>
                    <h3>@Model.Balance.ToString("N0") VND</h3>
                    <p>@Model.StartDate.ToString("dd/MM/yyyy") - @Model.EndDate.ToString("dd/MM/yyyy")</p>
                </div>
            </div>
        </div>
    </div>

    <div class="alert alert-info">
        <p>
            Đây là báo cáo dành cho quản trị viên. Admin chỉ có thể xem báo cáo tổng hợp,
            không thể tương tác trực tiếp với các giao dịch cá nhân của người dùng.
        </p>
    </div>
</div>